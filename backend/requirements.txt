# ─────────────────────────────────────────────────────────
# Scholarship Finder AI — Python Dependencies
# ─────────────────────────────────────────────────────────
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────
fastapi[standard]==0.137.1
uvicorn==0.49.0

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.51
alembic==1.18.4
psycopg2-binary>=2.9.9

# ── Validation ────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
python-multipart>=0.0.9

# ── Authentication ────────────────────────────────────────
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-dotenv>=1.0.1

# ── Task Queue ────────────────────────────────────────────
redis==8.0.0
celery==5.6.3

# ── AI ────────────────────────────────────────────────────
# Groq API via httpx (direct HTTP calls in ai_service.py)
# anthropic package removed — not used, Groq/Llama is the active provider

# ── Web Scraping ──────────────────────────────────────────
# Lightweight HTTP-based scraping (replaced heavy Playwright/Scrapy)
beautifulsoup4>=4.12.3
# Legacy scrapy kept for reference but no longer used in production
# scrapy==2.16.0
# playwright==1.60.0
# scrapy-playwright>=0.0.40

# ── Testing ───────────────────────────────────────────────
pytest>=8.2.2
pytest-asyncio>=0.23.7
httpx==0.28.1
PyPDF2>=3.0.0
slowapi>=0.1.9
apscheduler>=3.10.4

# ── Source Discovery (Producer) ───────────────────────────
# DuckDuckGo search — no API key required
duckduckgo-search>=6.0.0
